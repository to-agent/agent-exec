# SKILL: openclaw
# Endpoint: POST /api/exec
# Description: OpenClaw — AI coding agent

## Overview

OpenClaw is an AI coding agent. Run via `POST /api/exec`:

```json
{"args": ["openclaw", "-p", "<prompt>"]}
```

## Key Commands

```json
{"args": ["openclaw", "--version"]}
{"args": ["openclaw", "-p", "your task"]}
```

## Detailed Documentation

- [usage](references/usage.md) — Full flag reference
